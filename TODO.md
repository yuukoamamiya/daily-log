# Daily Log 后续计划

`v0.1.1` 已作为公开早期版本发布。项目主要由维护者个人使用，同时保持陌生用户可以零配置使用核心本地功能。没有规模化真实用户时，不把“收集反馈”当作交付目标；按实际使用中的问题和明确需求推进。

## 当前公开版最低承诺

- 全新数据目录可以启动。
- 账目、日记、待办和日程可以手动创建、修改、删除或恢复，并在重启后保留。
- SQLite 是唯一可写真源；投影、备份、网络和 Provider 失败不能破坏本地记录。
- 本地备份和恢复有安全副本与路径边界。
- AI、远程备份、日历订阅、模块同步和手机桥接都是可选能力，不影响零配置手动使用。

## P0：数据安全底线

- [ ] 持续检查 Release 产物不包含私人数据、密钥、绝对路径或测试数据库。
- [ ] 发现影响发布版数据安全、数据完整性或备份恢复的缺陷时，优先修复并补回归测试。
- [ ] 实际使用中发现核心缺陷后，优先修复并保留最小可复现测试；没有真实问题时不人为制造稳定性工作包。

## P1：现有核心可靠性

- [x] 完善并验收桌面托盘与退出状态机；自动化测试已覆盖。
- [x] 在真实代理环境中验证 WebDAV/S3 远程备份的连通性、认证、超时和失败重试；SOCKS5 暂不支持。
- [x] 完善 AI 历史记录复核：跨批次进度、失败重试和变更记录；建议确认后才批量写入。
- [x] 增加搜索、筛选和批量编辑；已完成自动化回归测试和隔离浏览器验收。
- [x] 在真实 PortableApps Platform 中验收 `.paf.exe` 的安装、升级、启动和 `Data` 保留。

## P1：Inbox 与手机桥接

现有本地自然语言录入已经支持 AI 生成统一 `plan` 并直接写入四个模块。新增工作是让处理过程可见、可异步化，并接入手机端。

- [x] 增加专门 Inbox 界面，展示自然语言到四个模块结构化结果的过程。
- [x] 复用现有 `plan` 和 `DailyLogDatabase` 写入路径；明确结果允许 YOLO 自动入库，歧义结果可编辑后保存。
- [x] 保证一次 Inbox 输入涉及多个模块时整体写入或整体失败。
- [x] 增加待处理、失败、重试和成功状态；成功后不要求长期保存原文。
- [x] 增加 `Provider + 来源项目 ID` 幂等记录，防止远程删除/关闭失败造成重复入库。
- [x] 设计独立手机桥接 Provider：分别支持“读取 Inbox”和“发布完整 Dashboard”；已提供内部接口和内置 Mock Provider。
- [x] 从 SQLite 生成四模块完整只读 Dashboard 快照，由后台异步发布；失败不阻塞本地保存，并支持重试。
- [ ] 评估第一个载体，例如 GitHub 私密仓库 + Issue、Simplenote 或其他具备访问控制的服务。
- [ ] 验证电脑离线时手机可继续录入，电脑下次启动后再拉取、处理并更新 Dashboard。

## P2：Provider 与模块同步

- [ ] 将 AI、备份存储、按模块同步和手机桥接抽象为内部 Provider；内置实现也通过同一接口调用，不立即开放第三方插件安装。
- [ ] 让每个模块 Provider 声明读取、创建、修改、删除、完成、标签同步和同步方向等能力。
- [ ] 配置上每种记录类型允许零个或一个活动 Provider，同时保留将来一对多扩展空间。
- [ ] 优先评估待办和日程双向同步；实现前明确 API、授权、同步方向、删除语义、重复检测、离线重试、历史 ID 映射、冲突解决、令牌保存、撤销授权和限流。
- [ ] 评估日记和账目的实际外部需求：接入真正有用的服务，或继续只提供本地记录、备份、导出和手机桥接，不为同步而同步。
- [ ] 为双向同步定义本地 UUID、外部 ID 映射、字段能力和冲突策略；SQLite 始终是真源。
- [ ] 只有在权限隔离、版本兼容、密钥访问、升级失败和错误隔离方案明确后，才评估公开插件机制。

## P2：核心能力完善

- [ ] 完善预算：按月覆盖、分类预算和历史预算变化。
- [ ] 为日历订阅增加刷新状态、上次成功时间和重复事件诊断。
- [ ] 根据实际使用补充安装、迁移和故障排查文档。
- [ ] 为本地 Agent Provider 设计可执行文件、参数、标准输入、超时、取消、输出大小限制和 JSON 校验边界。

## P3：实验性能力和非当前目标

- [ ] 重新评估导出目标软件，再决定保留、合并或删除现有格式。
- [ ] 调研 Codex CLI、OpenCode 等本地 Agent 的非交互调用和结构化输出能力，把它们作为可选 AI Provider。
- [ ] 实现实验性本地 Agent Provider，验证 Windows 路径、并发、失败提示和模型未安装时的降级体验。
- [ ] 只有在出现多个独立维护的扩展需求后，再设计插件清单、能力声明、启停、错误隔离和兼容版本。

以下个人功能不作为通用客户端当前版本目标，只有在不损害通用性的前提下再评估：

- [ ] 邮箱 Inbox。
- [ ] 维护者私人仓库的自动 Git 备份集成。
- [ ] 桌面便签与桌面小组件。
- [ ] 专用移动客户端；当前优先通过手机桥接 Dashboard/Inbox 解决手机查看和录入。
- [ ] Cloudflare 发布版复杂仪表盘。
