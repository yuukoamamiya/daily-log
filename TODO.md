# Daily Log 下一步计划

当前核心已经能独立保存记账、日记、待办和日程，并具备 AI、分类预算、日历订阅、备份恢复和基础导出。接下来停止扩张业务功能，优先完成公开发布所需的桌面体验与工程化。

## P0：首次使用与发布阻塞项

- [x] 增加首次使用引导：介绍本地数据位置，预算、AI 和远程备份均可跳过。
- [x] 为 AI、WebDAV 和 S3 设置增加“测试连接”，提供明确的失败原因。
- [x] 增加应用日志，日志自动遮蔽密钥和认证头。
- [x] 给服务增加可靠的单实例机制，不能只依靠固定端口判断。
- [x] 完成全新 Windows 用户目录的端到端验收：引导、录入、投影和本机备份。

完成标准：用户在不了解 Python、SQLite 或 Git 的情况下，也能首次启动、录入数据、找到数据位置并完成一次本机备份。

## P1：Windows 桌面封装

- [x] 使用 pywebview 封装当前 WebUI，不重写前端。
- [x] 增加托盘图标、打开主窗口、手动备份和退出操作。
- [x] 关闭窗口时允许最小化到托盘，后台 worker 与闲时备份继续运行。
- [x] 完成不依赖系统 Python 的 Windows 构建，并配置安装版与免安装绿色版产出。
- [x] 明确升级策略，确保升级程序不会覆盖 `%LOCALAPPDATA%/DailyLog`。
- [x] 增加版本号、关于页面、许可证和第三方依赖清单。

完成标准：下载解压或安装后可以直接启动，卸载程序不会删除用户数据，升级后旧数据库可以继续使用。

## P1：发布与质量保障

- [x] 增加只覆盖通用客户端的 CI，不依赖旧版命令行工具或仓库私人数据。
- [x] 增加 Windows 构建与干净虚拟机冒烟测试。
- [x] 使用 GitHub Actions 自动构建安装版和绿色版，普通提交只做验证，版本标签才创建正式产物。
- [x] 创建 GitHub Release 工作流，发布安装包、绿色版压缩包、SHA-256 校验和与更新说明。
- [x] 固定 Python 与构建依赖版本，保存构建日志和产物清单，确保本地与 CI 使用同一构建配置。
- [x] 确认发布打包范围不包含私人数据、密钥、绝对路径和测试数据库。
- [x] 对备份恢复、数据库迁移和 AI 网络边界再做一次安全审查。
- [x] 为高级用户提供可调整的数据目录：迁移前自动备份，校验路径边界，迁移后重启生效，并保持单实例保护。
- [x] 将日历日期点击改为当天详情面板：查看日记、账目、日程和待办；各内容进入对应查看/编辑流程，新增操作保持独立。
- [x] 增加 PortableApps 发布格式：按 PortableApps 规范制作可放入平台的绿色版压缩包和 `.paf.exe` 安装包，补齐 AppInfo、启动器、Data/DefaultData 目录约定、升级/卸载数据保护，并纳入 Windows CI 与 Release 产物验收。
- [ ] 在真实 PortableApps Platform 中人工验收 `.paf.exe` 的安装、升级、启动和 `Data` 数据保留；本机包结构、启动器冒烟和 ZIP 完整性已通过。

P1 说明：PyInstaller EXE、安装版、普通绿色版和 PortableApps 绿色版（ZIP / `.paf.exe`）均已纳入同一套 Windows 构建流程；普通绿色版默认把数据放在自身 `data\`，PortableApps 版把数据放在包内 `Data\DailyLog`，两者都附带只接受已有 `daily-log.db` 的 `DailyLog-HumanTest.cmd`（普通绿色版入口默认复用 `%LOCALAPPDATA%\DailyLog`）。安全审查覆盖备份 ZIP 路径/链接、显式迁移边界、AI/远程备份地址校验和密钥脱敏。当天详情面板、空目录数据迁移和重启指向已完成。本机 EXE 与 PortableApps 启动器已通过冒烟检查；正式安装包、PortableApps 安装/升级以及干净 Windows 环境仍由 Release/Windows CI 和人工验收完成。

## P2：核心体验完善

- [ ] 增加浅色、深色和跟随系统的主题切换，并记住用户选择。
- [ ] 评估并借鉴 [syhyz1990/mactype](https://github.com/syhyz1990/mactype) 油猴脚本的网页字体增强方案，将适合的 CSS/脚本能力内置到 `web/`，让浏览器版和 pywebview 客户端都改善字体可读性；不能要求用户安装油猴脚本或外部字体增强程序。
- [ ] 优化新增记账页一级、二级分类选择控件的视觉样式，使其与整体界面风格一致，避免呈现为未经设计的系统默认控件。
- [ ] 移除新增记账页金额输入框的原生上下调节按钮；保留数字校验和小数输入能力，但不提供容易误触且意义不大的逐分钱调节。
- [ ] 增加无标签、未分类记录的集中整理入口，并允许 AI 批量建议后直接应用。
- [ ] 完善预算：按月覆盖、分类预算和历史预算变化。
- [ ] 重新评估导出目标软件，再决定保留、合并或删除现有格式。
- [ ] 为日历订阅增加刷新状态、上次成功时间和重复事件诊断。
- [ ] 增加搜索、筛选和批量编辑。

## P2：扩展架构与本地 Agent

- [ ] 先把 AI、备份存储和 inbox 抽象成内部 Provider 接口，内置实现也通过同一接口调用；这一步只做模块化，不立即开放第三方插件安装。
- [ ] 评估公开插件机制是否真的必要，并形成架构决策记录。评估项包括版本兼容、插件发现、配置界面、权限、密钥访问、升级失败和恶意代码风险。
- [ ] 只有在出现多个独立维护的扩展需求后，再设计插件清单、能力声明、启停、错误隔离和兼容版本；首个正式版本不以第三方插件系统为发布条件。
- [ ] 调研 Codex CLI、OpenCode 等本地 Agent 的非交互调用和结构化输出能力，把它们作为可选 AI Provider，而不是让核心业务直接拼接命令行。
- [ ] 为本地 Agent Provider 设计明确的安全边界：可执行文件由用户选择、参数使用数组传递、输入走标准输入、禁止 shell 插值，并支持超时、取消、输出大小限制和 JSON 校验。
- [ ] 先实现一个实验性本地 Agent Provider，验证 Windows 路径、并发、失败提示和模型未安装时的降级体验，再决定是否作为正式发布功能。

## 暂缓的个人功能

以下功能在通用客户端首次发布后再做：

- GitHub Issue inbox 本地拉取、AI 处理与关闭 Issue。
- 邮箱 inbox。
- 当前私人仓库的自动 Git 备份集成。
- 桌面便签与桌面小组件。
- 手机远程访问或专用移动客户端。
- Cloudflare 发布版复杂仪表盘。

## 本轮完成的基础

- [x] SQLite 本地优先真源与 durable outbox。
- [x] 通用客户端不再隐式读取私人仓库数据。
- [x] 新用户默认空数据库。
- [x] 旧版仓库数据只能通过 `--migrate-from` 显式迁移。
- [x] 增加 `python -m daily_log` 独立源码入口。
