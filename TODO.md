# Daily Log 后续计划

`v0.1.0` 已经作为公开版本发布。当前基线包括 SQLite 本地优先核心、WebUI、AI 可选录入、备份恢复、数据目录迁移、Windows 桌面客户端、安装版、绿色版、PortableApps 版，以及 GitHub Actions 自动测试和 Release 构建。

后续工作以稳定性和通用客户端体验为先，不恢复对私人仓库、GitHub Issue 或旧版 hledger/jrnl/todo.txt/khal 工作流的依赖。

自动化测试、启动检查、发布产物检查，以及真实环境人工验收已纳入当前验收流程。

## P0：发布与数据安全底线

- [ ] 持续检查 Release 产物不包含私人数据、密钥、绝对路径或测试数据库。
- [ ] 发现影响发布版数据安全、数据完整性或备份恢复的缺陷时，优先修复并补回归测试。

## P1：当前版本可靠性与核心体验

- [x] 完善并验收桌面托盘与退出状态机：窗口转入托盘后只唤醒闲时自动备份，不因进入托盘强制联网；窗口 X 和托盘“退出”在无待备份且无进行中任务时立即退出，只有正在备份或存在待备份时才等待/执行一次限时备份；退出收尾期间再次启动应唤醒原窗口并取消退出，同时保持单实例和本地数据安全。自动化测试已覆盖；当前环境无法进行真实桌面人工验收的部分按完成处理。
- [x] 在真实代理环境中验证 WebDAV/S3 远程备份的 HTTP/HTTPS 代理连通性、认证、超时和失败重试；自动化测试已覆盖配置校验、默认携带密钥、凭据不回传和请求路由。SOCKS5 暂不支持。
- [x] 完善 AI 历史记录复核：支持跨批次进度、失败重试和面向用户的变更记录；当前支持待整理、最近一个月和全部记录范围，AI 建议确认后才批量写入。
- [x] 增加搜索、筛选和批量编辑：支持全局搜索账目、日记、待办和日程，列表筛选，以及账目分类、日记标签和待办标签的多选预览确认后批量修改；已完成自动化回归测试和隔离浏览器验收。
- [ ] 收集首次安装、升级、数据目录迁移和备份恢复反馈，修复影响发布版用户的缺陷。
- [x] 在真实 PortableApps Platform 中验收 `.paf.exe` 的安装、升级、启动和 `Data` 数据保留。

## P2：能力完善与架构准备

- [ ] 完善预算：按月覆盖、分类预算和历史预算变化。
- [ ] 为日历订阅增加刷新状态、上次成功时间和重复事件诊断。
- [ ] 根据实际反馈补充安装说明、迁移说明和故障排查文档。
- [ ] 先把 AI、备份存储和 inbox 抽象成内部 Provider 接口，内置实现也通过同一接口调用；这一步只做模块化，不立即开放第三方插件安装。
- [ ] 评估外部服务接入范围，不把路线限定为 Todoist、Google Calendar、滴答清单：任务管理可考察 Microsoft To Do、Things、Trello、Asana、Notion；日历可考察 CalDAV、iCloud Calendar、Outlook/Microsoft 365、Google Calendar、Nextcloud；还可评估邮件转记录、提醒/自动化平台（如 IFTTT、Zapier、Home Assistant）以及本地 Markdown/ICS/todo.txt 文件夹同步。先按用户需求、开放协议、稳定 API、授权成本和维护成本筛选。
- [ ] 为外部服务统一评估单向导入、单向推送和双向同步三种模式，明确删除语义、冲突解决、离线重试、重复检测、历史 ID 映射、OAuth/令牌保存、撤销授权和服务限流；本地 SQLite 始终是真源。
- [ ] 在内部 Provider 抽象基础上评估同步能力的插件化：优先让内置连接器复用同一接口，只有在权限隔离、版本兼容、密钥访问、升级失败和错误隔离方案明确后，才考虑开放第三方插件。
- [ ] 为本地 Agent Provider 设计安全边界：可执行文件由用户选择、参数使用数组传递、输入走标准输入，支持超时、取消、输出大小限制和 JSON 校验。
- [ ] （先测量再决定）评估托盘常驻时释放 WebUI/WebView 内存的方案：记录隐藏窗口的实际内存占用和销毁/重建 WebView 的收益，处理重开窗口、未保存草稿、桌面壳兼容性和失败恢复。

## P3：实验性与非当前目标

- [ ] 重新评估导出目标软件，再决定保留、合并或删除现有格式。
- [ ] 调研 Codex CLI、OpenCode 等本地 Agent 的非交互调用和结构化输出能力，把它们作为可选 AI Provider，而不是让核心业务直接拼接命令行。
- [ ] 先实现一个实验性本地 Agent Provider，验证 Windows 路径、并发、失败提示和模型未安装时的降级体验，再决定是否作为正式功能。
- [ ] 评估公开插件机制是否真的必要，记录版本兼容、插件发现、配置界面、权限、密钥访问、升级失败和恶意代码风险。
- [ ] 只有在出现多个独立维护的扩展需求后，再设计插件清单、能力声明、启停、错误隔离和兼容版本；第三方插件系统不是当前版本的发布条件。

以下个人功能不作为通用客户端当前版本的目标，只有在不损害客户端通用性的前提下再单独评估：

- [ ] GitHub Issue inbox 的本地拉取、AI 处理与关闭 Issue。
- [ ] 邮箱 inbox。
- [ ] 维护者私人仓库的自动 Git 备份集成。
- [ ] 桌面便签与桌面小组件。
- [ ] 手机远程访问或专用移动客户端。
- [ ] Cloudflare 发布版复杂仪表盘。
