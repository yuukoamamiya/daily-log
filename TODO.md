# Daily Log 后续计划

`v0.1.0` 已经作为公开版本发布。当前基线包括 SQLite 本地优先核心、WebUI、AI 可选录入、备份恢复、数据目录迁移、Windows 桌面客户端、安装版、绿色版、PortableApps 版，以及 GitHub Actions 自动测试和 Release 构建。

后续工作以稳定性和通用客户端体验为先，不恢复对私人仓库、GitHub Issue 或旧版 hledger/jrnl/todo.txt/khal 工作流的依赖。

## v0.1.x：发布后维护

- [ ] 在真实 PortableApps Platform 中人工验收 `.paf.exe` 的安装、升级、启动和 `Data` 数据保留。
- [ ] 收集首次安装、升级、数据目录迁移和备份恢复反馈，修复影响发布版用户的缺陷。
- [ ] 持续检查 Release 产物不包含私人数据、密钥、绝对路径或测试数据库。
- [ ] 根据实际反馈补充安装说明、迁移说明和故障排查文档。

## v0.2：核心体验完善

- [ ] 增加浅色、深色和跟随系统的主题切换，并记住用户选择。
- [ ] 改善浏览器版和 pywebview 客户端的字体可读性，不要求用户安装外部字体增强程序。
- [ ] 优化新增记账页一级、二级分类选择控件的视觉样式。
- [ ] 移除新增记账页金额输入框的原生上下调节按钮，同时保留数字校验和小数输入能力。
- [ ] 增加无标签、未分类记录的集中整理入口，并允许 AI 批量建议后直接应用。
- [ ] 完善预算：按月覆盖、分类预算和历史预算变化。
- [ ] 重新评估导出目标软件，再决定保留、合并或删除现有格式。
- [ ] 为日历订阅增加刷新状态、上次成功时间和重复事件诊断。
- [ ] 增加搜索、筛选和批量编辑。

## 后续架构与本地 Agent

- [ ] 先把 AI、备份存储和 inbox 抽象成内部 Provider 接口，内置实现也通过同一接口调用；这一步只做模块化，不立即开放第三方插件安装。
- [ ] 评估公开插件机制是否真的必要，记录版本兼容、插件发现、配置界面、权限、密钥访问、升级失败和恶意代码风险。
- [ ] 只有在出现多个独立维护的扩展需求后，再设计插件清单、能力声明、启停、错误隔离和兼容版本；第三方插件系统不是当前版本的发布条件。
- [ ] 调研 Codex CLI、OpenCode 等本地 Agent 的非交互调用和结构化输出能力，把它们作为可选 AI Provider，而不是让核心业务直接拼接命令行。
- [ ] 为本地 Agent Provider 设计安全边界：可执行文件由用户选择、参数使用数组传递、输入走标准输入，支持超时、取消、输出大小限制和 JSON 校验。
- [ ] 先实现一个实验性本地 Agent Provider，验证 Windows 路径、并发、失败提示和模型未安装时的降级体验，再决定是否作为正式功能。

## 暂缓的个人功能

以下功能不作为通用客户端当前版本的目标，只有在不损害客户端通用性的前提下再单独评估：

- GitHub Issue inbox 的本地拉取、AI 处理与关闭 Issue。
- 邮箱 inbox。
- 维护者私人仓库的自动 Git 备份集成。
- 桌面便签与桌面小组件。
- 手机远程访问或专用移动客户端。
- Cloudflare 发布版复杂仪表盘。
